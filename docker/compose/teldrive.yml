services:
  teldrive:
    image: ghcr.io/nishok000123/teldrive
    restart: always
    container_name: teldrive
    networks:
      - postgres
    volumes:
      - ./config.toml:/config.toml
    ports:
      - 8080:8080
networks:
  postgres:
    external: true
